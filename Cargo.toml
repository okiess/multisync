[package]
name = "multisync"
version = "0.1.0"
edition = "2021"
authors = ["Oliver Kiessler <kiessler@inceedo.com>"]
description = "A small CLI tool to pull multiple git repositories sequentially."
license = "MIT"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
dirs = "5.0"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
shellexpand = "3.1"

[profile.release]
opt-level = 3
lto = true
